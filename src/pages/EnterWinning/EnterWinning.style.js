import styled from '@emotion/styled';

const Styled = {
  NumberInput: styled.input`
    width: auto;
    padding: 0.4rem;
    margin-right: 0.4rem;
    font-size: 16px;
  `,
};

export default Styled;
