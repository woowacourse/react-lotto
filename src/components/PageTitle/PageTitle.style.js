import styled from '@emotion/styled';

const Styled = {
  PageTitle: styled.h2`
    font-size: 1.6em;
    margin: 0 0 1em 0;
    color: #707070;
    font-weight: normal;
  `,
};

export default Styled;
